---
title: "Next.js App Router 완벽 가이드"
description: "Next.js 15의 App Router 핵심 개념과 서버/클라이언트 컴포넌트 활용법을 정리합니다."
date: 2026-06-20
tags: ["Next.js", "React", "TypeScript"]
category: "프레임워크"
published: true
---

## App Router란?

Next.js 13에서 도입된 **App Router**는 파일 시스템 기반 라우팅의 새로운 방식입니다.

## 서버 컴포넌트 vs 클라이언트 컴포넌트

### 서버 컴포넌트 (기본값)

```tsx
// 서버에서 실행 — async/await 직접 사용 가능
export default async function Page() {
  const data = await fetch('https://api.example.com/data')
  return <div>{data}</div>
}
```

### 클라이언트 컴포넌트

```tsx
'use client'

import { useState } from 'react'

export default function Counter() {
  const [count, setCount] = useState(0)
  return <button onClick={() => setCount(count + 1)}>{count}</button>
}
```

## generateStaticParams

정적 경로를 미리 생성합니다:

```typescript
export async function generateStaticParams() {
  const posts = getPublishedPosts()
  return posts.map((post) => ({ slug: post.slug }))
}
```

## 마치며

App Router는 처음엔 낯설지만, 서버/클라이언트 경계를 명확히 나누면 성능과 유지보수 모두 향상됩니다.
