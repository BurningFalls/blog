---
title: "첫 번째 블로그 포스트"
description: "Next.js 15 + velite + Tailwind CSS로 만든 기술 블로그의 첫 번째 포스트입니다."
date: 2026-06-21
tags: ["블로그", "Next.js", "velite"]
category: "일반"
published: true
---

## 블로그를 시작하며

이 블로그는 **Next.js 15**, **velite**, **Tailwind CSS**로 만들어졌습니다.

## 기술 스택

- **프레임워크**: Next.js 15 (App Router)
- **콘텐츠**: velite + MDX
- **스타일링**: Tailwind CSS v4
- **배포**: Vercel

## 코드 예시

```typescript
// velite로 포스트 가져오기
import { posts } from '@/.velite'

export function getPublishedPosts() {
  return posts
    .filter((p) => p.published)
    .sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime())
}
```

앞으로 다양한 기술 관련 포스트를 작성할 예정입니다.
